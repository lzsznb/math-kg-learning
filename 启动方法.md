# 项目启动说明

## 前置要求

- Python 3.10+
- Node.js 18+
- 后端依赖安装：

```bash
cd backend
pip install fastapi uvicorn networkx openai langchain-openai langgraph langchain-core
```

- 前端依赖安装：

```bash
cd frontend
npm install
```

## 启动项目

**1. 启动后端**（在项目根目录下）：

```bash
uvicorn backend.main:app --host 0.0.0.0 --port 8080
```

**2. 启动前端**（在 frontend 目录下，新开一个终端）：

```bash
cd frontend
npm run dev
```

**3. 访问**

浏览器打开 http://localhost:5173 即可使用。

> **注意**：tools.py里面的AI 问答功能需要 DeepSeek API，请在环境变量中设置 `DEEPSEEK_API_KEY`，如有需要请联系 1784238233@qq.com。
